AZURE_OPENAI_ENDPOINT=https://oai-co-nop-pocpca.openai.azure.com/
AZURE_OPENAI_API_KEY=<TU_API_KEY>
AZURE_OPENAI_DEPLOYMENT=o4-mini
AZURE_OPENAI_API_VERSION=2024-12-01-preview

# Modo estricto: si no hay conexión/salida JSON válida => error (502)
AI_STRICT=true
AI_ENFORCE_JSON=true
AI_MAX_TOKENS=2000

# Si tu empresa usa proxy (opcional; recomendado si estás tras proxy)
# HTTPS_PROXY=http://usuario:password@proxy.empresa.com:puerto
# HTTP_PROXY=http://usuario:password@proxy.empresa.com:puerto

# Si hay inspección TLS, indica el certificado corporativo:
# SSL_CERT_FILE=C:\ruta\corporate-root-ca.pem
# REQUESTS_CA_BUNDLE=C:\ruta\corporate-root-ca.pem
